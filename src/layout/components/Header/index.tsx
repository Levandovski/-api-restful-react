import React from 'react';

import { ContainerHeader, HeaderContent } from './styles';

const Header: React.FC = () => {
	return (
		<ContainerHeader>
			<HeaderContent>Teste</HeaderContent>
		</ContainerHeader>
	);
}

export default Header;
